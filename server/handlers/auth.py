"""Handlers de autenticacao (Sprint 1, Dia 3).

handle_register(data, sock) -> valida, cria user, retorna sucesso/erro
handle_login(data, sock)    -> verifica hash, cria sessao, retorna user info
handle_logout(data, sock)   -> limpa sessao
"""

# TODO(Sprint 1, Dia 3): register / login / logout.
